.row.toprow.header
  .large-12.columns
    - if !@auth.nil?
      #username= link_to(@auth.username + ' | logout', '/login', :method => 'post', :class => 'logout')
  .large-12.columns
    - if @auth.games
      %table.recordtable
        %th Game
        %th Score
        %th Best Word
        - @auth.results.each do |result|
          %tr
            %td= result.game.name
            %td= result.score
            %td= result.game.best_word



.row
  .large-12.small-12.columns
    .row
      - if !@auth.nil?
        .large-4.columns
          #username= link_to(@auth.username + ' | logout', '/login', :method => 'post', :class => 'logout')
        .large-4.columns.large-centered
          = link_to('Enter Game', new_game_form_path, :class => 'button gamebuttons', :remote => true)


.row
  #form
